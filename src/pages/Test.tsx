import { Button } from "@/components/ui/button";

const Test = () => {
  return (
    <div>
      <Button variant="default" size="lg">
        Text
      </Button>
    </div>
  );
};

export default Test;
